==> Authentication
    => register // api create in routes
        (1. user ka data save krna,
         2. token user ko dena )
    => login
    => logout (token blacklisting)
    => [OTP based registration]

==> Post
    => create
        {
            caption:String,
            img_url:String,
            user:userId,
            createdAt:Date-time
        }
    => can see the feed
    => like posts (collection types)
    => save post

==> users
    => followers
    => following
    
    user = { // Schema creat in model
        username : String,
        email : String,
        password : String,
        bio : String,
        profile_image : String
    }

